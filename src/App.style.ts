import type { SystemStyleObject } from "@chakra-ui/react";

const styles = {
  page: {
    height: "100dvh",
    width: "100vw",
  },
} satisfies Record<string, SystemStyleObject>;

export { styles };
